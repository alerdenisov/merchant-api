export abstract class BlockchainController {
  // load_balance(currency: string) {}
  // load_transaction(txid: string) {}
  // create_address(options?: any) {}
  // create_withdrawal(issuer: any, recipient: any, amount: any, options?: any) {}
  // normalize_address(address: string) {}
  // normalize_txid(txid: string) {}
}
